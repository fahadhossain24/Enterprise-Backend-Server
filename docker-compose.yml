services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: enterprise-backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    environment:
      - NODE_ENV=production

    restart: unless-stopped
